Use this skill when adding a new CRM resource module to the Raynet n8n node.
It defines the canonical file structure, naming conventions, export contracts,
and registration steps that every new resource module must follow.
See module-templates.md for the full specification and templates.
